{% extends 'base.html.twig' %}

{% block title %}Chaussure index{% endblock %}

{% block body %}
    <section class="mt-24">
        <p>Nombre de chaussures : {{ nbChaussures }}</p>
        {% for chaussure in chaussures %}

        <img src="{{ chaussure.image }}" class="w-40 h-40">
        <p>{{ chaussure.nom }}</p> 
        <p>{{ chaussure.prix }}</p> 
        <a href="{{ path('app_chaussure_show', {'id': chaussure.id}) }}">Afficher</a>
        
        {% endfor %}

    </section>

    <a href="{{ path('app_chaussure_new') }}">Create new</a>
{% endblock %}
