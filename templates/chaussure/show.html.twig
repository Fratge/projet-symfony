{% extends 'base.html.twig' %}

{% block title %}Chaussure{% endblock %}

{% block body %}
    <h1>Chaussure</h1>

    <table class="table">
        <tbody>
        <tr>
          <td>{{ chaussure.nom }}</td>
          <td>{{ chaussure.prix }}</td>
          <td><img src="{{chaussure.image}}" class="w-40 h-40"></td>    
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chaussure_index') }}">back to list</a>

    <a href="{{ path('app_chaussure_edit', {'id': chaussure.id}) }}">edit</a>

    {{ include('chaussure/_delete_form.html.twig') }}
{% endblock %}
